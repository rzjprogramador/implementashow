import { resolversRoot } from "../routers/root.resolvers.ts";
import { typeDefsRoot } from "../routers/root.typedefs.ts";

const typeDefs = typeDefsRoot
const resolvers = resolversRoot

export { typeDefs, resolvers  };