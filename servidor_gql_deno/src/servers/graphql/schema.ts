import { resolversRoot } from "./root.resolvers.ts";
import { typeDefsRoot } from "./root.typedefs.ts";

const typeDefs = typeDefsRoot
const resolvers = resolversRoot

export { typeDefs, resolvers  };